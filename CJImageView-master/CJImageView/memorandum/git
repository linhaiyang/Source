
回滚就是反向修改回去，只会回滚 当前这次 提交的内容。

重置(git reset)

---选中要重置到的提交，右键“将xxx重置到这次提交”。----本地有这次提交的内容
混合合并：被撤销掉的提交的内容会保留在工作区，就是 git reset 没有 hard 参数效果
强行合并：被撤销掉的提交的内容不会保留在工作区，就是 git reset 有 hard 参数效果
//
5. 分支合并 --选中要合并过来的分支，右键“合并xxx至xxx”。

//变基
切换需要变基的分支，右击master 选择变基到master

忽略文件.gitnore


/**
CocoaPods 自 1.8 版本开始默认使用 trunk CDN (https://cdn.cocoapods.org/) 作为 spec 的源，本意是非常好的，
避免了需要本地 clone 一份庞大的 Specs 仓库导致每次 update 都要全量更新的问题。然而不知为何国内连 trunk CDN 都被限制访问了。

